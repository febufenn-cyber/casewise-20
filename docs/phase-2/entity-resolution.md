# Entity resolution contract

Observed source text, entity candidates, and canonical entities are separate layers. Name similarity creates review proposals only. Conflicting identifiers or entity types block automatic merges.

Roles are contextual to proceedings and time; an entity does not receive one permanent legal role. Every merge preserves mention assignments and source entity state so it can be undone without reconstructing history from logs.
